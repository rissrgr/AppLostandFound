<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:theme="@style/Base.Theme.LostAndFound">

    <group android:menuCategory="container">
        <item
            android:id="@+id/mainMenuFilter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:title="Filter"
            app:showAsAction="ifRoom" />
        <item
            android:id="@+id/mainMenuProfile"
            android:icon="@drawable/ic_person"
            android:title="Profile"
            app:showAsAction="never|ifRoom" />
        <item
            android:id="@+id/mainMenuFavoriteLostandFounds"
            android:title="Marked Items"
            app:showAsAction="never" />
        <item
            android:id="@+id/mainMenuAllData"
            android:title="All Data"
            app:showAsAction="never" />
        <item
            android:id="@+id/mainMenuMyData"
            android:title="My Data"
            app:showAsAction="never" />
        <item
            android:id="@+id/mainMenuLogout"
            android:title="Logout"
            app:showAsAction="ifRoom" />
    </group>

</menu>
